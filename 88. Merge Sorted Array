var merge = function(nums1, m, nums2, n) {
    let j = 0
    for(let i = m;i<nums1.length;i++){
        nums1[i] = nums2[j]
        j++
        }
    // nums1.splice(m)
    // nums1.push(...nums2)
    nums1.sort((a,b) => a-b)
};
